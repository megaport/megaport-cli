# Docker Configuration
PORT=8080
TZ=UTC

# Session Management
# How long customer sessions last before requiring re-authentication
# Valid values: 30m, 1h, 2h, 24h, etc.
SESSION_DURATION=1h

# Note: Customer credentials are NOT stored in environment variables
# Each customer logs in with their own credentials through the web UI
# Sessions are managed securely server-side with tokens in the browser
